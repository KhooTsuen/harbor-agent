# 踩坑记录

> 这份是**细节**。规矩本身在 [`../AGENT.md`](../AGENT.md)（那份每轮都会被
> 注入对话，所以要短；细节放这里，需要时才翻）。

每一条都是真踩过的，带当时的报错或现象。**改代码前扫一眼，能省几次返工。**

---


| 坑 | 症状 | 教训 |
|---|---|---|
| **模块名被同名参数遮住** | `config.hasKey is not a function` —— 发消息直接失败 | 函数形参叫 `config` 会把 config **模块**遮住。内核里模块要另起名（如 `configCore`）。`tsc` 查不到（`.cjs` 不过类型检查） |
| **Python heredoc 里 `\n` 会变成真换行** | 改完 JS/TS 报 `Invalid or unexpected token` | 写文件内容时别用 `\\n`，用 `chr(92)` 拼，或改完立刻 `node --check` / `tsc --noEmit` 验一遍 |
| **grep 品牌名要大小写不敏感** | 以为清干净了，用户截图里还有「Codex」 | `grep codex` 匹配不到 `Codex`。清理品牌一律 `grep -i` |
| **残留检查会变成挡路的门** | 单人对话发不出消息，弹「找不到项目」 | 一个变量取出来检查完就再没用过 → 说明那个条件已经失效。重构时要问「这个检查现在还代表真实约束吗」 |
| **按行切片重构容易切多** | 一次 `python` 替换把相邻的 5 个函数一起搬走了 | 切之前先看首尾锚点之间**有什么**，切完立刻跑 typecheck |
| **React state 同帧不更新** | 键盘连按箭头只挪一步 | 连续输入的场景要用 ref 累加当前值，别读 state/props |
| **两套限位打架** | 分隔线"下限"漂到 87px，看着像没夹住 | 同一件事只让一层说了算；像素限位和百分比限位不要各夹一遍 |
| **npm 11 拦 install 脚本** | node-pty 装完没有二进制 | 手动跑 `node node_modules/node-pty/scripts/post-install.js`；`npm run pty:check` 能验 |
| **原生模块没进便携版** | 开发时好用，打包后终端打不开 | `dist/` 只有前端 bundle；原生模块要在 `build-portable.mjs` 里单独拷 |
| **PTY 用内置 conpty 会 fork 崩** | 控制台刷 `AttachConsole failed` 堆栈 | 传 `useConptyDll: true` 走包内 DLL，跳过那条路径 |
| **重构提示词时"漏层"** | 模型说「我的系统提示里没有时间信息」。tsc / lint / 265 项测试**全过** | 分层重构只保证结构，**不保证内容还在**。加「内容回归」测试逐项断言（见 selftest 的「系统提示 / 内容回归」组），并做变异测试确认它真能红。**能通过不算测试，能失败才算** |
| **拆函数时留空占位** | 界面能开、tsc 全绿，但流式内容/工具过程/引用全不更新 | 主流程里留一个 `handleStreamEvent(){}` 就以为拆完了。**跨层接线是测试照不到的地方** —— 拆完必须真的跑一遍（`npm run shot:electron` 发一句话） |
| **`Partial<Store>` 当返回值** | store 合起来报「类型不可赋值」 | 返回 `Partial<AppState>` 会让动作变成可选，和必选声明对不上。用 `Pick<AppState, '分支动作名...'>` |
| **gitignore 写 `/* */` 注释** | 差点把根目录所有文件都忽略掉 | `.gitignore` 只认 `#` 注释；`/*` 是一条**模式**（匹配根目录下所有东西） |
| **真机探针跑在启动动画上** | 探针返回全是 `false`，DOM 里 `buttonCount: 0`，正文只有 `HARBOR / LOCAL AGENT SIGNAL` | `BootSequence` 覆盖整屏（`fixed inset-0 z-[90]`）且要 **~8 秒**才让出界面，而真机工具 2500ms 就执行脚本。**探针要先轮询等 `.harbor-boot` 消失**，别点一下就往下走（点它只记一条 click，动画照样要走完）。判断「是不是卡住了」看 `data/logs/actions-<日期>.jsonl` —— 那里面能看见渲染层每一步 IPC |
| **中文后面不能写 `\b`** | `^(设置)\b` 这条正则**永远不匹配**，设置按钮就在眼前却点不到 | JS 的 `\b` 按 `\w`=`[A-Za-z0-9_]` 算，中文字符不是 word char，所以中文后面的 `\b` 两端都是「非词」→ 没有边界。**匹配中文用精确选择器或 `^…$`，别用 `\b`** |
| **断言钉在「怎么写」而不是「做了什么」** | 一次无害重构（5 处订阅模板收成一个 `subscribe()` 帮手）让 3 条自检变红，而功能没坏 | `preloadSrc.includes("ipcRenderer.on('app:taskEnd'")` 这类断言把实现细节当成了需求。**改成对意图成立**：`/(?:ipcRenderer\.on\|subscribe)\(\s*'app:taskEnd'/`。否则每次重构都要改测试，改着改着就没人看测试了 |
| **PowerShell 重定向写出的日志是 UTF-16** | 用 `node x.mjs > log` 落日志，再用 node 按 `utf8` 读 → **一行都匹配不到**（内容是 `p\0r\0e\0`） | PS 5.1 的 `>` 默认 UTF-16LE。**要么让 node 自己 `writeFileSync(...,'utf8')`**，要么读的时候 `readFileSync(f,'utf16le')`。同一份日志用 grep 搜得到、用 node 搜不到，就是这个原因 |
| **CI 红了 13 次没人发现** | GitHub Actions 从 v1.11.0 起一路红叉，本地 `npm run verify` 却全绿 | 本地验证链和 CI **不是一套**：CI 多一步 `prettier --check`（一开就报出 23 个文件格式不符）。两边的清单必须逐项对齐 —— 已把 `check:format` 接进 `verify`。**「本地绿」只说明本地绿** |
| **自检断言写死了 Windows 语义** | 补完格式后 CI 卡在自检：ubuntu 上红两条 —— `canon：大小写与尾斜杠算同一个目录`、`同名目录不会登记两条` | `canon` 按平台分开处理（Windows 大小写不敏感 / Linux 敏感）**是对的**，错的是断言只写了 Windows 那半边。**跨平台代码的断言要按平台分支写**（或挑两边都成立的例子），并且要真在 CI 上跑过 |
| **测试里两条任务落在同一毫秒** | 同一个提交 CI 时红时绿（#56 绿、#57 红）：自检的 `报告里指出「之前跑过一次」` 失败 | `findPrior` 只认**严格更早**的 createdAt —— 同毫秒创建的两条互不算「之前」。真实使用不会踩（两次跑至少隔几秒），但**测试得自己隔开时间**（`await sleep(5)`），别赌机器速度。本地复现：`--freeze` 把 Date.now 冻住，报告尾和 CI 一模一样 |
| **上游的流会「挂住」** | 真机验收 24 次里 3 次整轮对话永远停在「运行中」：请求发出去了、一个字节都不回来，也不报错（连"停止"都靠外部 180 秒判超时才收场） | 流式请求需要一个**空闲看门狗**：两个数据块之间超时（现定 45 秒）就主动放弃、报成可重试的 `timeout`，交给既有重试/降级链路（见 `electron/core/llm-idle.cjs`）。`http.cjs` 有意不设**总**超时（正常流可以跑几分钟）—— 看门狗管的是「卡住」，不是「慢」。自检 `79-llm-stream` 用「吐一块就挂住」的假流钉住它 |
| **`cn()` 会把自定义字号类当颜色吞掉** | `Row` 标签写着 `text-dense`，真机量出来是 **15px 继承值** ——”写了字号却不生效“，样式看着像随机生效 | tailwind-merge 不认识 `tailwind.config.js` 里的自定义字号名（meta / body / title / dense），把 `text-dense` 归成**颜色类**，后面跟 `text-fg-primary` 时被覆盖。**自定义 scale 要注册进 twMerge 的 classGroups**（见 `src/lib/utils.ts`），并加单测钉住 |
| **设置页容器层不带 gap，卡片会贴死** | 数据与用量页四张删除卡间距 **0px**（肉眼像一整块）、扩展页技能卡 0~1px，而「通用」页好好的 8px | 这几页的 tab 组件包了 `<div className="py-1">`（无 gap），把 `.settings-body` 的 8px 弹性节奏切成普通块流；返回 Fragment 的页没这个问题。**设置页的容器层要么返回 Fragment，要么自带 `gap-2`**。探针 `tmp/parse-metrics.cjs` 能直接列出相邻卡片的间隙 |
| **重读会话会换掉消息 id** | 「切走再切回来它又自己生成」；真机日志里点一次「上一版」就紧跟一个 `chat:send`（连点四次 = 四次） | `openFromDisk` 重读后每条消息都是**新 uid**（`storedToUi` 里 `uid('msg')`）——拿重读前的 id 查必然失败，失败又被当成「这一版没回答过」而自动重跑。**跨重读的引用一律按签名找回**（内容 + 版本表，timestamp 消歧）；写盘要写回**磁盘 key**（`Message.diskKey`），写新 uid 会在文件里多一条同名提问副本（真机探针逮到的）。jsdom 里 `loadSession` 返回 null → 不重载 → id 恰好没变，**单测照不到**：要 mock「重载且换 id」的形状 + 真机对照（老构建复现 `chat:send`、新构建同操作安静） |
| **初始化期取循环导入的绑定 → TDZ** | 只有「直接 `import` 某个子模块」的测试报 `Cannot access '__vite_ssr_import_6__' before initialization`，其他测试/构建/CI 全绿 | `useThreadStore ↔ turns` 本来就是循环；新加的 `regenerate` 让 `messageVersions` 在 **create(...) 执行路径上**碰了还没初始化的导出，导入顺序换个入口就炸。**循环图里的模块，导出只在调用期取**（别在模块初始化期调用/展开）；另外**留一个直接 import 子模块的测试口子**，不然这种问题只会在某一条入口上偶发 |
| **动作流里同一次 IPC 记两行** | 查「到底跑了几轮」时被行数误导：一次 `chat:send` 在 `actions-*.jsonl` 里是**两行**（preload 的 `call()` 记一行 + `register-handlers` 的 ipcMain 包装记一行），ms 也各测各的） | 计数历史事件时别数行数 —— **同 name、ts 相邻（<数十 ms）的两行就是一次调用**；判「是不是多跑了」看渲染层点击次数与成对行数。真实例：beta.4 一次版本切换的「chat:send ×2」其实是**一次**发送（bug 依然成立，但「×2」的口径错了） |
| **任务台账没按会话过滤** | 在对话 A 里跑任务、中途手动中断，新建对话 B —— B 会「接着把 A 的活干完」，仿佛任务状态是全局共享的 | `task-context.cjs` 的 `buildTaskState` 之前把**全局**未完成任务（本会话的 + 别人会话的）拼在一起注入，还附「接着计划里第一条没有 [x] 的往下做」——B 的第一轮请求里就躺着 A 的目标/计划/脚印。**任务上下文的每一处（注入/恢复/检查点/启动扫描/门禁）都必须按 `sessionId` 过滤**；「兜底注入别人的任务」是最像贴心设计的那种 bug（自检 82 组当场就红了）。真机验证用「paused 任务在盘上 → 新建对话」：`chat:send=0` 且任务文件逐字节不变。另有口径坑：**恢复入口**要按聊天列（`main`）统计 —— 右栏任务中心是显式全局管理器（AG-012），那里列出所有任务的「继续」不算泄漏；启动清单提示是 bootstrap 异步弹的，探针要 `await ~2.5s` 再抓（800ms 抓不到，会误报「没提示」） |
| **回答的版本标签被「重贴」** | 一条提问先答 A（重新生成 5 次），编辑成 B 后 B 的切换器显示 1/8、1/9（把 A 的 6 条也数进来），切回 A 又一条都不剩 | 编辑/重新生成时，旧回答交给新一轮（seedAnswers）前会被重贴 `answersVersion` —— 已标过的一律**只补缺、不重贴**（标签是它生成时就定下的）。同族还有两个坑：`allAnswers` 里「自己」跟着 `message.content` 漂（切走会把它盖成上一条的内容 —— 切走前要先把原文定格进台账）；`toAnswerRecord` 的 key 要用**磁盘 key**（重读会给消息换新 uid，用 uid 和台账里的自己对不上 → 重开后每个答案多一条 N+1）。真机对照：同一夹具旧构建 1/4、6/7，新构建 1/3、6/6（探针 `tmp/iso7-run.cjs`） |
| **「消息树 / parentId / activeLeafId」不是这套数据的形状** | 需求按「树形结构」描述（每条消息有 id 和 parentId、activeLeafId 记叶子）；照着去找/造这些字段会全部对不上 —— 这套代码里根本没有 | Harbor 的分叉对应物：**提问多版本** = 同一条用户消息的 `versions[]`/`versionIndex`；**回答多版本** = `answerRecords`/`answersVersion`/`answerIndex`；**后续链跟着当时那一版走** = 记录上的 `parentKey`/`parentVersion`（内核筛）。「激活路径」本来就持久化在这些字段里（每个提问记 versionIndex、每个位置记 answerIndexByVersion）—— **零迁移**。另一个边界：**内核只读激活路径**（非激活版本的后续轮次不在内存里）—— 所以「分支树」v1 只画当前路径上的分叉点 + 各分叉的兄弟分支，画全树要改读取口径（beta.9 已按此实现） |
| **浮层定位：夹视口 ≠ 翻转** | 分叉浮层在锚点靠近容器顶时**压在面包屑上**（越出滚动容器）、滚动 +120 面板位移 **0**、锚点滚到底面板**还挂在 top=13px** 处遮挡内容（真机探针 12 轮夹具复现） | 手写 `place()` 把坐标**夹进视口**而不是**翻转方向**，滚动重算的结果每次都被夹回同一个死点。定位交给 `@floating-ui/react`：`autoUpdate`（祖先滚动/缩放/尺寸/布局位移）+ `flip` + `shift({crossAxis:true})` + `hide()`（锚点被完全裁掉 → 自动关闭）。修复后同夹具：面板恒在容器内、滚动位移 **-120 = 锚点位移**、滚出视野自动关（beta.11）。**浮层用 `left/top` 别用 `transform`** —— 和 motion 的缩放动画会互相覆盖 |
| **jsdom 的布局数字全是假的** | Popover 单测里 `hide()` 误报 `referenceHidden=true`（面板一开就自己关）；浮层尺寸怎么量都是 **0×0** | jsdom 的 `document.documentElement.clientWidth/Height` 是 **0** → floating-ui 视口退化成 0×0 → 什么都被认为「完全裁掉」；它量**浮层自身**尺寸走的是 `offsetWidth/offsetHeight`（不是 `getBoundingClientRect`），jsdom 里恒 0。**测试要自己补布局**：`defineProperty` 补 viewport 尺寸 + 用 WeakMap mock 各元素的 rect/offset 尺寸（见 `popover.test.tsx`）。另：`AnimatePresence` 退出动画（~140ms）期间面板还在 DOM 里，**断言「已消失」要先 `vi.waitFor`**，否则 flaky |
| **Tailwind 3 对纯 `var()` 颜色静默丢弃 `/NN`** | 全仓 30+ 处 `bg-bg-raised/40`、`bg-bg-base/95`、`border-danger/40`（危险按钮、任务行、内嵌面板、悬浮条…）**从来没有渲染出背景** —— 不报错、tsc/lint/单测全绿，主题轮量真机时才逮到；「层级感弱」有一部分就是它 | 颜色要支持 `/NN` 必须写成 `rgb(var(--x-rgb) / <alpha-value>)`（空格分隔通道值，放各主题的 index.css / 主题文件）；发现线索：**产物 CSS 里直接搜那个类名，搜不到就是没生成**。已把 `bg-canvas / surface / raised / hover / danger` 五个换成通道模式 + 单测钉住（`tailwind.config.js` 顶部有注释，改背景色时 `-rgb` 要跟着改） |
| **启动时「还在跑」的任务会被内核标成暂停** | 探针夹具写 5 种任务状态验颜色：running / waiting_user 的行全变成灰「已暂停 · 上次被中断」，跟要验的蓝/黄对不上 | 这是**设计**（单实例：启动时不可能真有循环在跑，见 `task.cjs` 的 `pauseRunning`）。要在真机看到「运行中蓝 / 需确认黄」：启动后用桥**运行时改写**再点「刷新任务」—— `window.workbench.taskUpdate({ id, patch: { status: 'running' } })`；这比改夹具更接近真实（顺便证明了这个修改路径本身能用） |
| **真机探针的三个测量口径** | ① 主区底色量到 `rgba(0,0,0,0)`（颜色在祖先上）② 发送按钮刚启用量到**旧色**（React 刷新 + 150ms 过渡没走完）③ 分叉菜单 `[data-fork-menu]` 量到透明（底色在**父级** glass 面板上） | ① 向上取第一个**不透明**背景再比色；② 轮询到目标色再断言（`sleep` 固定时长不可靠）；③ 属性节点 ≠ 视觉面板 —— 底色看 `parentElement`/`.closest('.glass')` |
| **git 夹具放进了仓库里** | 自检新组「不是 Git 仓库的目录要降级」在本地怎么都红：夹具目录明明空着却报出 `branch: main` | 夹具建在自检沙箱（`projects/selftest/…`）里，而那目录**在仓库工作树内** —— `git -C` 沿父目录一路向上找到 `.git`，空目录也是「仓库」。**「不是仓库」的夹具必须放系统临时目录**（`fs.mkdtempSync(os.tmpdir())`），别和被测仓库共用祖先 |
| **prettier 会把不合格式的 self-test 撑爆** | 给 32-notify 组加 3 行后顺手 `prettier --write`，文件从 299 行变 **339** —— 直接越过 300 行红线；而它本来就不在 prettier 名单里 | `scripts/selftest/groups/32-notify.mjs` 的历史缩进不符合 prettier，**格式化 = 全文件重排**。把某文件收进格式化范围前先看 diff 大小；踩了就 `git checkout` 回滚、重做最小编辑，别顺着重排去拆文件 |
| **语义色禁令是活的（会扫字面量）** | 新开屏组件用了 `var(--warning)`、`var(--success)`，单测当场红：组件里禁止出现语义色字面量 | `src/lib/statusLanguage.ts` 是语义色唯一定义处，执法测试扫描 `var(--success\|danger\|warning)`。**新组件要色一律走 `colorOf('waiting'/'failed'/'completed'/'warning')`**；`var(--accent-blue)` 这类主题 token 不受限（灯塔光束就走它） |
| **没有凭据时灯塔「灯灭」是设计，不是 bug** | 真机探针开屏画面 `beams=0`，差点当缺陷报 | `Lighthouse` 状态由 `credentialsStatus()` 决定：**一个密钥都没配 → offline（灯灭）**，这是诚实降级（设计稿要求「状态层必须真实」）。要看光束/呼吸态得先在真机里配一个 provider；探针环境是干净便携版数据目录，所以永远是 offline |
| **`export type {…}` 带 `from` 会让上方 import 变未使用** | 把 `safety.ts` 的一条转发改成 `export type { … } from './task'` 后，`tsc` 报 TS6196 + eslint 报 unused —— 明明正文里还用着 `TaskBridge` | `export … from` 是**转发**，不在本地绑定名字；上方 `import` 里没被正文使用的名字就报废了。**import 只留正文真用的**，其余直接 `export type {…} from './task'` 转发 |
| **自检偶发 EPERM 重跑一次就好** | `npm run verify` 第 6 步内核自检偶发红：`EPERM: operation not allowed, rename 'projects-selftest.json.tmp'` | Windows 上索引器/杀软偶尔扣着刚写出的临时文件不放。**同一条命令立刻重跑若全绿，按抖动处理**（不影响提交）；真能复现再查。别为它满仓库加 sleep |
| **悬停三连坑：撑高 → 换内容 → 时隐时现（三版才定稿）** | 同一个侧栏行连报三轮：① 悬停时长高（32 → 40px）；② 悬停换内容（时间/模式藏起来、按钮顶出来，右半边跳）；③ 按钮做成「悬停渐显」后用户要求**常显** | 行里的操作入口（如「更多操作」）：**别用悬停藏/露**。定稿规矩：槽子钉死高度（`h-4` + `items-center`，防撑高）、占地恒定（`w-7`，防位移）、按钮**不加任何悬停门**（防复现/消失）；悬停只许动「底色/描边」这类装饰。钉子测试 `rowHoverHeight.test.ts`（扫描前先剥块注释） |
| **注释里拼出 `*/` 会提前终止块注释** | 写「剥注释」的测试帮手，注释里解释「斜杠星号…星号斜杠」时写成反斜杠形式 —— 其中的「星号+斜杠」序列仍把块注释**当场截断**，剩下的中文变成代码 → 语法错误（tsc 报一长串） | 块注释里**永远不要拼出「星号紧跟斜杠」这两个字符**（加反斜杠也不行 —— 反斜杠不是转义）。要描述它就用文字：「斜杠星号 / 星号斜杠」。同理：写含 `*/` 的正则时，它在字符串/正则里安全，在**注释里**才致命 |
| **合成 mouseover 事件触发不了 CSS `:hover`** | 探针在 DOM 里 `dispatchEvent(new MouseEvent('mouseover'))` 后量行高 —— 尺寸纹丝不动（Δ=0、按钮 0×0），看着像「悬停没有副作用」 | CSS `:hover` 是浏览器原生状态，JS 合成事件不改变它 —— Tailwind 的 `group-hover` 当然也不响应。**探针要强制 :hover 用 CDP `CSS.forcePseudoState`**（拿 DOM.getDocument + querySelector 获 nodeId → forcedPseudoClasses:['hover']），末尾再真移一次鼠标（`Input.dispatchMouseEvent: mouseMoved`）做交叉验证 |
| **vitest 半路硬崩（0xC0000409）也是抖动** | `npm run verify` 第 5 步 exit `3221226505`，日志停在某文件的 act() 警告处：**没有 FAIL 行、也没有 Test Files 汇总** | 0xC0000409 = `STATUS_STACK_BUFFER_OVERRUN`，是 Node/worker 级崩溃而非断言失败（全链高负载时偶发）。**先单跑 `node tmp/unit-all.cjs`**：全绿就重跑全链，按抖动处理；看到具体 FAIL 行才当缺陷查 |
| **CDP `Browser.close` 在托盘应用上永久挂起** | 探针想「优雅退出」，`await cdp.send('Browser.close')` **永远不返回**（应用 minimizeToTray，关窗口只是藏起来）—— 探针整段卡死、任务控制台还看不到输出 | 托盘型应用上别指望 `Browser.close` 收尾。**CDP 的每个调用都要带超时**（自己包一层 Promise.race / setTimeout）；退出老老实实 `taskkill /F /PID`（`/T` 带子进程）。「关闭窗口」和「退出应用」在这类应用里是两回事 |
| **强杀会丢还没落盘的 localStorage 写入（约 5 秒才提交）** | 探针把 localStorage 值改掉（读回也是新值）→ 马上 kill → 冷启动读到的还是**旧值**（磁盘上旧记录仍在） | Chromium 的 localStorage 提交是**批量异步**的（实测约 5s 一批）。「改完就杀」的正确姿势：**同时写一个唯一 token 到另一个键 + 直接扫 leveldb 等 token 出现**，出现即本批已提交，再杀。token **每段都要新的**（复用会在后段 0ms 误报「已落盘」）。leveldb 在 `data/chromium/Local Storage/leveldb`，`.log`/`.ldb` 是明文 |
| **应用启动后的自身设置写会覆盖你刚改的值** | 探针冷启 1 秒内把 `bootAnimation` 改成 true，token 都落盘了，冷启回读却仍是 false —— 应用启动收尾的 `syncFromConfig` 按**内存里的旧值**整份写回，把补丁盖掉 | 给「跑着的应用」改持久化设置：① 先等它**静置**（主界面起来 + 2~3s）；② 写完**回读确认**；③ 被覆盖就重试（本次实测第 1 次就成）。「token 落盘」只证明批次提交过，**不证明你的值还在** |
| **圈码 ①②③ 不能进 JS 标识符** | `out.①落盘等待 = ...` → `SyntaxError: Invalid or unexpected token`（同一个坑连踩三次，每次废掉一轮探针） | `①②③`（U+2460+）是「数字符号」类，不是字母/数字，**不能做标识符**。标识符老实写（`out.落盘等待一`）；字符串/注释里随便用。tmp 脚本写完先 `get_errors` / `node --check` 再跑 |

---

## 一个容易误解的设定

**「默认工作目录」只影响没有自己目录的对话。**
在设置里改了它，已有对话不会跟着变 —— 它们的目录记在自己的会话文件里
（侧栏「对话文件夹」就是按这个分的）。这是设计，不是 bug；
排查「为什么 Agent 看的还是旧目录」时先看这条。

想让自己在这个仓库里干活时读到上面这些规矩：
把工作目录设成仓库根目录即可 —— `AGENT.md` 会被自动注入。

---

## 行数欠债：已清零

2026-09-14 晚把所有超标文件拆完了。**现在 `electron/`、`scripts/`、`tools/`、`src/`
里没有任何一个超过 300 行的文件**（`.ts` / `.tsx` / `.cjs` / `.mjs` 全算）。

拆分产物（新增的文件，都在 300 行以内）：

| 原来的 | 拆成 |
|---|---|
| `electron/core/loop.cjs` 576 | `loop.cjs` / `loop-prompt.cjs` / `loop-tools.cjs` / `loop-model.cjs` / `loop-route.cjs` |
| `electron/main.cjs` 611 | `main.cjs` / `selftest-report.cjs` / `tray.cjs` / `window-state.cjs` / `handlers/workdir.cjs` / `handlers/provider.cjs` |
| `scripts/selftest.mjs` 1193 | `selftest.mjs`（清单）/ `selftest/harness.mjs` / `selftest/env.mjs` / `selftest/groups/01..06-*.mjs` |
| `electron/core/session.cjs` 372 | `session.cjs`（门面）/ `session-io.cjs` / `session-read.cjs` / `session-write.cjs` |
| `electron/core/mcp.cjs` 364 | `mcp.cjs`（连接池）/ `mcp-connection.cjs` |
| `electron/core/tools/index.cjs` 373 | `tools/index.cjs`（执行）/ `tools/registry.cjs` / `tools/permission.cjs` |
| `electron/core/memory.cjs` 317 | `memory.cjs`（门面）/ `memory-recall.cjs` |
| `electron/core/memory-store.cjs` 306 | `memory-store.cjs` / `memory-schema.cjs` |
| `tools/shot.mjs` 368 | `shot.mjs` / `shot/cdp.mjs` / `shot/scenarios.mjs` |
| `electron/core/task.cjs`、`electron/handlers/fs.cjs` | + `task-plan.cjs`、`fs-text-ext.cjs` |

### hook 里存状态 + 被两处调用 = 静默失效

`useBulkSelect()` 内部用 `useState`，「多选删除」按钮在 `SidebarHeader`、
列表在 `Sidebar` —— 两个组件各调一次就各拿到一份独立状态。点按钮只改了
其中一份，另一份读到的永远还是初始值。**tsc、eslint、151 项测试全绿**，
只是功能没了。

**规则：一个 hook 只要可能被两处调用，状态就不能存在它自己里面**
（用 zustand store / context）。这个 bug 是我把按钮从 `Sidebar.tsx` 抽成
`SidebarHeader.tsx` 时引入的 —— 抽之前两者同属一个组件，所以没事。

**测这类共享状态不能用 `renderToStaticMarkup`**：那是服务端渲染，
zustand 走 `getServerSnapshot`，永远返回初始值。要真挂 DOM（jsdom 有了），
断言「从一处改，另一处也能看到」。

### 拆分这个活儿本身踩到的坑（都是真的）

**① 拆一半比不拆更糟。** 在 `turns.ts` 里把 139 行的 `switch (event.type)`
整块摘掉、只留了个空的 `handleStreamEvent(){}` 占位 —— tsc 全绿、146 项前端测试全过，
但流式内容、工具过程、引用**全都不再更新**。层与层之间的接线，测试照不到。

**② 按「下一处 `}`」当边界，会切多。** 切 `main.cjs` 的托盘段时，末尾多带走了
`for (turn)` 的闭合括号；切 `tools/index.cjs` 时把 `...makeMessageActions(set)`
一起卷走 —— 那会让 store **悄悄丢掉所有消息动作**，而且 tsc 照样过。

**③ 按关键词找锚点，会找到注释里的那一个。** 找「单实例」时命中了文件开头注释里的
「跳过单实例锁」，结果把节标题插到了文件顶部。**同一个坑踩了三次**，后来改成
「先定位基准行，再在该行之后搜索」。

**④ 从注释块中间开始切，会切出一个没头没尾的 `/*`。** 把 `loop.cjs` 的路由段搬到
`loop-route.cjs` 时，起点落在了块注释的第二行，于是新文件开头是个裸的 `* 路由：…`，
原文件留了个孤立的 `/*`（它把后面整段代码注释掉了，而 `node --check` 还是过）。

**⑤ 搬走的模块里，谁 require 谁要重新理。** 「搬过去的函数引用了留在原地的常量」
这个错误一次都没被静态检查挡住：`CHROME is not defined`（截图工具）、
`ctx is not defined`（测试拆分）、`tools is not defined`、`ipcMain is not defined`。
**`.cjs` 不过 tsc，`node --check` 只查语法不查未定义变量** —— 只能靠真跑。

**⑥ preload 不能拆 —— 它的注释和测试都写着，而我没读到位。**
2026-09-24 为了让 `preload.cjs` 回到 300 行以内，我把「记忆」那一组桥方法
抽成 `preload-memory.cjs` 再 `...require(...)(call)` 展开。tsc 绿、lint 绿、
630 个文件全合规 —— **但内核自检里有一条专门盯这个的断言直接红了**：

> ★ preload 是自足的（不许拆到兄弟文件里 require）—— sandbox:true 下要求兄弟模块会抛，整个 preload 作废

`main.cjs` 开的是 `sandbox: true`，沙箱化的 preload **不允许 require 自己的模块**；
那次 require 一抛，`window.workbench` 整个不存在，**界面所有 IPC 全死**。
更值得记的是：`preload.cjs` 文件头第 15 行**早就写着这段**，而且写着
「真机探针逮到的」—— 也就是说**这个坑以前踩过一次**，我既没读到注释、也没先搜测试。

**规则：动一个文件之前，先看那个文件开头有没有「别动这里」的注释，再 grep 一下
有没有盯着它的断言。** 行数超了要压，宁可压注释（`--code-only` 口径下这些文件
本来就是合规的：超的全是注释），也别拆 preload、main、以及任何运行时受限的文件。

### 自检会往真实 `data/` 里写东西（2026-09-24 清出 647 条）

排查别的问题时顺手数了一下 `data/tasks/`：**647 个任务文件**，
582 条是 `failed` 的「你好」（goal 空、sessionId 空）、60 条 `selftest*`、
其余 5 条看名字也是测试造的 —— **一条用户的活都没有**。

两个根因：

**① 跑 loop 的测试组，在「抛错路径」上从不清理。** `08-limits.mjs` 会真跑一次
`loopCore.run`，而循环是**先建任务再执行**；用量闸把这一轮拦下时它直接抛错，
调用方拿不到 `taskId` —— 于是每次都留一条。攒了几个月。
`05-agent-loop.mjs` 有一趟「故意让模型挂掉」的调用更彻底：连 `goal` / `sessionId`
都没传，留下的那条连名字都没有。

**② `removeSafe` 拒绝删「在跑」的任务 —— 而测试建出来的任务默认就是 `running`。**
`finally { taskCore.removeSafe(id) }` 看着做了清理，其实一条没删。
（内核拦得对：删了还在跑的任务会变成幽灵任务。错的是测试以为自己删掉了。）
`taskCore.remove(id)` 没有这个限制，所以老测试组没事 —— 问题只在用 `removeSafe` 的地方。

现在统一走 `scripts/selftest/env.mjs` 的 `disposeTasks(ids)`：先把状态改成 `cancelled`
（测试的任务不可能真在跑），再删；返回删不掉的那些，好让测试自己断言。
`76` / `77` / `06` / `05` / `08` 五个组都改过来了，并钉了一条
「测试建的任务都删干净了」。

**顺带一条规矩**：那 647 条是**挪**走的（`data/backups/tasks-residue-*/`），不是删 ——
这个项目一贯如此：拿不准的东西先留着，留个 README 说明是什么、为什么挪。

### 两件直接改进验证纪律的事

**① `--self-test` 曾经查不出「handler 没注册上」。**

主进程注册 handler 的那段是**同步**跑的，`app.whenReady()` 早在它之前就挂好了。
于是中间随便哪一行抛错（真实案例：新建的 `handlers/provider.cjs` 用了 `ipcMain`
却忘了 `require('electron')`），**它后面的 handler 全部不注册，但窗口照常打开**，
Electron 只弹一个「A JavaScript error occurred in the main process」。
自检当时照样全绿 —— 因为它只查「已经能查的」，查不到「哑掉的那些」。

现在自检会清点通道：`electron/ipc-channels.cjs` 的 `EXPECTED_CHANNELS` 写死一份
「应该存在的通道」清单（2026-09-23 实测 **115** 条；加通道要同步它），
挨个检查注册情况，输出 `channelsOk` / `channelsMissing`。
（查法有讲究：`ipcMain.handle` 注册的通道**不在** `eventNames()` 里，
`listenerCount` 对它也恒为 0；真正存 handle 的是私有的 `_invokeHandlers` Map，
留了退回 `listenerCount` 的退路。）

**② 冒烟测试曾经从不触发「用量累加」那条路径。**

Agent 循环冒烟测试里 stub 的 `llm.chatStream` 直接 `return`，从不调 `options.onUsage`
—— 于是 `loop.cjs` 里 `mergeUsage` 因为拆分漏了 import，**315 项照样全绿**，
真发一句话才报 `mergeUsage is not defined`。现在 stub 会主动调一次 `onUsage`。

**共同点：一次「真的跑一遍」比一堆通过的测试管用。**

## 风险分级：三个「模式表看着挺像样」的假象

2026-09-23 例行排查时随手写了个 21 条命令的探针，**14 条判错**。三个根因都不是
「黑名单少写了几条」——那种补不完，`risk.cjs` 开头自己就写着这句话。

**① 只审命令开头 = 后面跟什么都能过。**
`LOW` 白名单全是 `^\s*(ls|dir|find|grep|…)`，只匹配**开头**。
`find . -delete` 开头是 `find` → 整条判成 **low** → 在「中风险放行」的配置下
**静默递归删除**。21 条里最吓人的就是这条。
**规则：白名单的语义必须是「整条都像只读」，不是「开头像只读」。**

**② 一张模式表只覆盖一种 shell 方言。**
表里全是 cmd / unix 写法，于是 `format c:` 拦得住、**`Format-Volume` 拦不住**；
`shutdown /s` 拦得住、`Stop-Computer -Force` 拦不住（原正则要求参数带**斜杠**，
PowerShell 用的是 `-Force`）；`schtasks /create` 有、`/delete` 没有。
账户那几条更是**完全没有** —— 而 `net localgroup administrators x /add` 等于拿下整台机器。
**规则：写"危险命令"这类表时，先把同一个动作在 cmd / PowerShell / unix 里的写法各列一遍。**

**③ 兜底写在调用方，自己反而不说 → UI 会说谎。**
`decide()` 在 `high: 'allow'` 时返回 `allow`，真实确认全靠 `tools/index.cjs` 里
一条 `|| verdict.level === 'high'` 顶着。于是设置页的风险试算显示「**直接执行**」，
实际执行却弹确认框 —— 同一个判断，两个地方说法不一样。
**规则：一条规则要写在"拥有这条规则"的那个函数里，别让调用方替你兜。**
（顺带：这条也解释了一个 UI 文案 bug —— critical 显示成「危险**风险**」，
因为标签表给的是「危险」、调用处又字面拼了个「风险」。同一个道理：拼接发生在错的地方。）

**④ 把新逻辑提到更高等级，要连「别误伤」一起验。**
`grep -r`、`ls -f`、`find -name` 和破坏性开关长得很像。把 `-r` / `-f` 收进
「破坏性参数」表，就会误报一堆正常命令 —— 而**误报的代价是这个分级被用户整个关掉**，
那比不修还糟（人不会去改一个天天拦自己的东西，只会关掉）。
所以自检里「**没治过头**」和「治住了」是同等重要的验收项：
`npm install` 仍是中风险·直接执行、`grep -r` 仍是低风险·直接执行，都写进断言。
