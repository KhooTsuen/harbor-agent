# 环境变量示例

复制成 `.env` 或 `.env.local`，按需修改。所有变量都有默认值，不设也能跑。

```bash
# ═══ 运行模式 ═══

# 仅 Vite 开发服务器可用：是否使用 mock 回复做 UI 开发预览。
# 便携版/生产构建永远使用真实 Electron 后端，不会被此变量切成演示模式。
VITE_USE_MOCK=0

# ═══ 开发 ═══

# Vite 开发服务器端口
DEV_PORT=5273

# Electron 是否自动打开 DevTools（0 = 关）
DEVTOOLS=0

# ═══ 打包 ═══

# 便携版输出目录（默认 dist-portable/）
PORTABLE_OUT_DIR=

# 应用版本号（写进 package.json 和「关于」页）
VITE_APP_VERSION=0.1.0
```

> 注意：API Key **不要**放这里。它在 Electron 版里填，存到本机 `data/config.json`。
