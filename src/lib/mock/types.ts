/* ══════════════════════════════════════════════════════════════
   模拟层共用的最小接口

   只写这里真正用到的字段，不直接复用 Project —— 模拟层不该依赖
   数据模型的全貌，这样模型改了也不会连带炸掉。
   ══════════════════════════════════════════════════════════════ */

export interface MockProject {
  name: string
  path: string
  branch: string
}
